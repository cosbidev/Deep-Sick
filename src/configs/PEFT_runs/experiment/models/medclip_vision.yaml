backbone: MEDCLIP_vision

n_last_blocks:  [1, 4]

avgpools: [True, False]

modules_target:
  - query
  - value
  - keys
  - dense

modules_target_same_shape:
  - query
  - value
  - key

feedforward_modules:
  - dense