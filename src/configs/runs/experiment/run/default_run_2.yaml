# Adding String for experiment name
exp_id_str: running_experiment_2

# Number of epochs to checkpoint
save_checkpoint_frequency: 1

# Interval to EVAL the model
eval_period_epochs: 3

# Number of epochs for testing on validation:
val_epochs: 1000

# Number of epochs for testing on test:
epochs: 5
# Number of epochs for early stopping
early_stop_epochs: 100

# Number of epochs for warmup training
warmup_epochs: 50


